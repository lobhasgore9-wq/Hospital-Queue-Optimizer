import { createFileRoute } from '@tanstack/react-router';
import { doctors } from '@/lib/demo-data';
export const Route = createFileRoute('/dashboard/doctors')({ component: () => <div className="space-y-4"><h1 className="text-2xl font-bold text-foreground">Doctor Workload</h1><p className="text-sm text-muted-foreground">Monitor doctor availability, patient load, consultation speed, and schedule across departments.</p><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-4">{doctors.map(d=><div key={d.id} className="rounded-xl border border-border bg-card p-4"><div className="flex items-center justify-between mb-2"><span className="text-sm font-semibold text-foreground">{d.name}</span><span className={`text-xs px-2 py-0.5 rounded-full ${d.status==='active'?'bg-success/10 text-success':d.status==='break'?'bg-warning/10 text-warning':'bg-destructive/10 text-destructive'}`}>{d.status}</span></div><div className="text-xs text-muted-foreground">{d.department}</div><div className="flex justify-between mt-2 text-xs text-muted-foreground"><span>{d.patients} patients</span><span>~{d.avgTime} min/patient</span></div><div className="mt-2 h-1.5 rounded-full bg-muted"><div className="h-1.5 rounded-full bg-primary" style={{width:`${Math.min((d.patients/15)*100,100)}%`}}/></div></div>)}</div></div> });
