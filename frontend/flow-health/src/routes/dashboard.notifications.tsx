import { createFileRoute } from '@tanstack/react-router';
import { notifications } from '@/lib/demo-data';
import { Bell } from 'lucide-react';
export const Route = createFileRoute('/dashboard/notifications')({ component: () => <div className="space-y-4"><h1 className="text-2xl font-bold text-foreground">Notification Center</h1><p className="text-sm text-muted-foreground">All patient alerts, system notifications, and operational updates in one place.</p><div className="space-y-2">{notifications.map(n=><div key={n.id} className={`flex items-start gap-3 rounded-xl border bg-card p-4 ${!n.read?'border-primary/30 bg-primary/5':'border-border'}`}><div className={`h-9 w-9 rounded-lg flex items-center justify-center shrink-0 ${n.type==='urgent'?'bg-destructive/10 text-destructive':n.type==='complete'?'bg-success/10 text-success':'bg-primary/10 text-primary'}`}><Bell className="h-4 w-4"/></div><div><div className="text-sm font-medium text-foreground">{n.title}</div><div className="text-sm text-muted-foreground">{n.message}</div><div className="text-xs text-muted-foreground/60 mt-1">{n.time}</div></div></div>)}</div></div> });
